import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.softyfy.app',
  appName: 'SoftyFy',
  webDir: 'dist',
  backgroundColor: '#121212',
  android: {
    backgroundColor: '#121212',
  },
}

export default config