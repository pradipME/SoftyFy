spring:
  application:
    name: softyfy
  profiles:
    active: ${SOFTYFY_PROFILE:local}

server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true
